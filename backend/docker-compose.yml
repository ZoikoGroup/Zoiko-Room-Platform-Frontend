services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: zoiko
      POSTGRES_PASSWORD: zoiko
      POSTGRES_DB: zoiko_rooms
    ports:
      - "5435:5432"
    volumes:
      - zoiko_pgdata:/var/lib/postgresql/data

volumes:
  zoiko_pgdata:
